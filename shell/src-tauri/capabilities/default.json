{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions granted to Stage / Credits windows. Allowlist for B1 (Shell <-> Stage)",
  "windows": ["stage", "credits"],
  "permissions": ["core:default"]
}
