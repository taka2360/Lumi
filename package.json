{
  "name": "lumi",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "dev": "pnpm --filter @lumi/shell dev",
    "build": "pnpm build:core && pnpm --filter @lumi/shell build",
    "build:core": "cd core && uv run pyinstaller lumi-core.spec --noconfirm",
    "stage:dev": "pnpm --filter @lumi/stage dev",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
