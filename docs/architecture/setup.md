# 初回セットアップと外部エンジンの取得

> **このファイルが唯一の定義場所であるもの**: 外部エンジンの取得元のピン留め、取得物の検証方法、
> インストール先、ロールバックの意味、既存インストールの検出、TTS セットアップ状態。
>
> **何を配布物に含めるか**は [../licensing.md](../licensing.md) が唯一の定義場所。ここは「含めないものをどう取ってくるか」。

親: [DESIGN.md](../DESIGN.md) / 決定: [ADR-019](../decisions/ADR-019-tts-engine-distribution.md)

| | |
|---|---|
| 確定度 | **Confirmed**（原則・状態）/ Provisional（ピン留めするバージョンと URL） |
| 対象 Phase | Phase 0 |

---

## 1. 原則

[ADR-019](../decisions/ADR-019-tts-engine-distribution.md) から来る4つ。**どれも実装で曲げない。**

| # | 原則 | 具体的な意味 |
|---|---|---|
| 1 | **ユーザーが選ぶまで外部通信しない** | 起動しただけでは 1 バイトも外に出さない。取得の起点は常にユーザーの操作（Network-optional の最初の適用例） |
| 2 | **取得する / しない を同等に提示する** | 「後で」を小さく灰色にしない。**既定は「取得しない」** |
| 3 | **部分的にインストールされた状態を残さない** | 確定は最後の 1 手（ディレクトリの rename）。失敗したら一時ディレクトリごと消える |
| 4 | **黙って劣化しない** | 取得しなかった / 失敗したは、どちらも「TTS 未セットアップ」として画面に出る。無音で動き続けない |

> **「取得しない」は壊れている状態ではない。** ただし Lumi は起動せず、セットアップ未完了の
> `blocked` 画面に留まる〔[ADR-034](../decisions/ADR-034-gate-startup-on-complete-setup.md) で変更〕。
> 「セットアップが完了していない」と「取得に失敗した」を、UI でもログでも区別する
> （[core.md](core.md) §6「セットアップされていない場合と、起動に失敗した場合を区別する」）。
>
> **原則2は維持する。** ボタンは同じ大きさ・同じ重さで並べ、拒否側を灰色にしない。
> 変わったのは選択肢の対等さではなく、「取得しない」の**帰結**である
> （拒否すると、セットアップ未完了の `blocked` 画面に留まり、ユーザーはそこから [終了] を選べる）。

---

## 2. TTS セットアップ状態

Core が持つ。Stage は `stage.*` で受け取って**表示するだけ**。

```
                    ┌──────────────┐
   起動 ───────────▶│   unknown    │  まだ調べていない
                    └──────┬───────┘
                           │ 検出（ローカルのみ。外部通信しない）
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
   ┌────────────┐   ┌────────────┐   ┌──────────────┐
   │  detected  │   │ installed  │   │not_configured│
   │ 既存のエンジン│   │ Lumi が入れた│   │  未セットアップ │
   └────────────┘   └────────────┘   └──────┬───────┘
                                             │ ユーザーが「取得する」を選ぶ
                                             ▼
                                      ┌────────────┐  失敗  ┌────────┐
                                      │ installing │───────▶│ failed │
                                      └─────┬──────┘        └────────┘
                                            │ 成功                │ 再試行できる
                                            ▼                     │
                                      ┌────────────┐◀─────────────┘
                                      │ installed  │
                                      └────────────┘
```

`failed` は `not_configured` に戻さない。**「試して失敗した」と「まだ試していない」は違う**（前者は理由を出す）。

### 導入の状態と、プロセスの状態を混ぜない〔Phase 0 で追加〕

上の状態機械が表すのは**導入されているか**であって、**いま喋れるか**ではない。
エンジンのプロセスの状態（`stopped` / `starting` / `ready` / `failed`）は**別の軸**として持つ。

```
installed  ×  stopped   → 入っているが、まだ起動していない
installed  ×  starting  → 起動中（初回はエンジン自身のモデル取得で数分かかる）
installed  ×  ready     → 喋れる
installed  ×  failed    → 入っているのに起動できない = **壊れている**
```

**1つの enum に混ぜると「入っているのに起動できない」を表現できず、
ユーザーに「取得してください」と嘘の案内をすることになる。**
プロセスの所有と生存の規則 → [core.md](core.md) §6「所有と生存」

### 完了するまで、毎回そこから再開する〔[ADR-034](../decisions/ADR-034-gate-startup-on-complete-setup.md) で変更〕

**「答えたかどうか」を永続化しない。** `%LOCALAPPDATA%\Lumi\setup.json` は廃止した。

覚える意味は「同じことを毎回聞かない」ことにあり、これはセットアップが**任意**だったから成立していた。
必須になった以上、答えを覚えることは**「不足したまま二度と聞かない」と同義**であり、
ユーザーは毎回「セットアップは完了していません」だけを見せられて終わる。**出口の無い状態を作らない。**

鬱陶しさは、別の手段で抑える。

| 抑えるもの | 手段 |
|---|---|
| 同じ起動の中で聞き直すこと | **Lumi の側からの聞き直しはしない。** 失敗後の再試行は**ユーザーが押したときだけ**（§2b） |
| セットアップ済みの要素を聞くこと | `not_configured` の要素だけを尋ねる（揃っていれば1つも尋ねない） |

### UI の置き場所（TTS）

**Phase 0 では `stage` ウィンドウの中のパネルとして出す**（独立ウィンドウにしない）。
理由と当たり判定の扱い → [ui.md](ui.md)「初回セットアップ UI は `stage` ウィンドウの中に置く」

---

## 2b. LLM / STT のセットアップ状態〔Phase 1。決定 → [ADR-023](../decisions/ADR-023-llm-runtime-and-model-acquisition.md) / [ADR-035](../decisions/ADR-035-separate-stt-installation-and-runtime-state.md)〕

**§1 の4原則は3つとも同じ。取得方法だけが違う。**

| | LLM（Ollama） | STT（faster-whisper モデル） | TTS（AivisSpeech） |
|---|---|---|---|
| Lumi が取得するか | **しない**（検出のみ） | **する**（同意に基づく） | **する**（同意に基づく） |
| 理由 | インストーラが可逆でない。他社ソフトの導入判断を代行しない | サイズが R1 に直撃する（数百 MB〜） | [ADR-019](../decisions/ADR-019-tts-engine-distribution.md) |
| 未導入のとき | 「LLM 未セットアップ」＋ 公式サイトの案内 | 「STT 未セットアップ」＋ 取得の提案 | 「TTS 未セットアップ」 |

**Silero VAD（ONNX）は配布物に同梱するので、セットアップ状態を持たない。**
barge-in は中核機能であり、「取得するまで遮れない」状態を作らない。

### 導入の状態（LLM）

**プロセスの状態と混ぜない**（§2 の規則をそのまま適用する）。

| 導入の状態 | 意味 | ユーザーに求める行動 |
|---|---|---|
| `unknown` | まだ調べていない | — |
| `not_configured` | **Ollama が見つからない** | 公式サイトからインストールしてもらう |
| `detected` | Ollama が見つかった | — |
| `model_missing` | **Ollama はあるが、設定されたモデルが無い** | `ollama pull <model>` を案内する |

| プロセスの状態 | 意味 |
|---|---|
| `stopped` | 127.0.0.1 の API が応答しない |
| `ready` | 応答する。**推論できる** |
| `failed` | 応答するが異常（バージョン不整合など） |

> **「入っているのに動かない」を1つの enum で潰さない。** `detected × stopped`（入っているが起動していない）と
> `not_configured`（そもそも入っていない）では、ユーザーに求める行動がまったく違う。
> ここで嘘の案内をすると、ユーザーは Lumi ではなく自分の環境を疑い始める。

### 導入の状態（STT）

TTS と**同型**（`not_configured` → `installing` → `installed` / `failed`）。
取得は §3〜§5 の経路をそのまま使う（ピン留め + サイズ + SHA-256 + 原子的なインストール + ロールバック）。

`failed` は**モデル取得の失敗**だけを表す。取得済みモデルを CTranslate2 がロードできない場合は
`installed × runtime: failed` であり、取得失敗には戻さない（[ADR-035](../decisions/ADR-035-separate-stt-installation-and-runtime-state.md)）。

| Provider の状態 | 意味 |
|---|---|
| `stopped` | まだモデルをロードしていない |
| `starting` | CTranslate2 がモデルをロード中 |
| `ready` | 推論できる |
| `failed` | モデルは取得済みだがロードできない |

**ライブラリ既定の自動ダウンロードは無効にする。** `faster-whisper` / `huggingface_hub` は
モデルが無ければ黙って取りに行くため、原則1（ユーザーが選ぶまで外部通信しない）が
**ライブラリの都合で迂回される**。キャッシュに無ければ明示的に失敗させる。

置き場所は `paths.models_dir()` = `<data_dir>/models/`〔Phase 1 Step E〕。
**エンジン（`engines/`）と分けている**のは、消す単位が違うため — モデルだけ捨てて
取り直したい状況が普通にある（壊れた / 別のサイズに替える）。

**Silero VAD はこの経路に乗らない。** 配布物に同梱してあり
（[../licensing.md](../licensing.md) §4.6）、取得は一切発生しない。
barge-in の critical path に「初回は動かない」を作らないためである。

### 起動フェーズへの反映〔[ADR-034](../decisions/ADR-034-gate-startup-on-complete-setup.md) で全面的に変更〕

Phase 0 の `boot` は TTS だけを見ていた。**LLM / STT / TTS の3つから導出する。**
導出は引き続き決定論的な純粋関数（[ui.md](ui.md)「起動フェーズ」）。

**3つが揃うまでキャラクターを出さない。** 聞く（STT）・考える（LLM）・喋る（TTS）の
どれか1つでも欠ければ会話が成立せず、**キャラクターが立っているのに反応しない**＝壊れて見えるため。

**上から順に見る。順序そのものが規則である。**

| # | 条件 | `boot` | なぜ |
|---|---|---|---|
| 1 | 質問を出している | `setup` | 答えを待っている |
| 2 | **TTS か STT を取得中** | `installing` | 進捗があり、**ユーザー自身が選んだ処理**が進んでいる |
| 3 | 導入の不足・取得失敗、LLM が使えない、または Provider の起動・ロード失敗 | `blocked` | **待っても揃わないと既に分かっている。** 不足項目と解決方法を出す |
| 4 | 導入済みの TTS / STT が `stopped` / `starting` | `starting` | Core が起動・ロード中。終われば `ready` に届く |
| 5 | 3つとも使える | `ready` | — |

#### 3 が 4 より先にある理由〔2026-08-20 修正〕

**待ち画面を出してよいのは、待ち終わったら `ready` に届くときだけである。**

音声認識モデルを「今は取得しない」と答えた時点で、この起動でセットアップが完了しないことは
**もう決まっている**。それでもエンジンの起動を先に見ると、ユーザーは
「AivisSpeech を起動しています…」を**2分間見せられた末に**「セットアップは完了していません」と言われる。
**答えは最初から分かっていたし、そのときユーザーはまだ画面の前にいた**〔実際に起きた〕。

**2 が 3 より先にあるのは、取得が「ユーザー自身がいま選んだ処理」だから。**
本人が押したダウンロードの進捗を隠して一覧を出すのは、それはそれで
「いま何が起きているか」について嘘をつくことになる。

> **エンジンの起動自体は止めない。** 画面の規則を変えただけで、裏では起動を試み続ける。
> **待たないことと、見ないことは別である** — エンジンが起動できなければ、それも
> 不足項目として同じ画面に出る（次の起動まで気づかない、を作らない）。
> ただし**起動中であることは `blocked` の画面に出さない**。ユーザーに何も求めない項目であり、
> 「次のものが揃うと起動できます」の下に並べれば4つ目の待ち事項に見える。

「使える」の定義（3つで違う）:

| | 使える |
|---|---|
| TTS | `detected` / `installed` **かつ** プロセスが `ready` |
| LLM | `detected` **かつ** プロセスが `ready` |
| STT | `installed` **かつ** Provider が `ready` |

#### 待機画面で待たせてよい条件は1つだけ — **「今まさに使えるようになる」**〔Phase 1 Step G〕

この規則は残る。**`installing` / `starting` は待機画面**（進捗があり、終われば使えるようになる）であり、
`blocked` は待機画面ではなく**やることが書いてある画面**なので、この規則の対象外である。

**`blocked` で待たせているのではない。** 待っているのはユーザーの行動であって、Lumi の処理ではない。

#### `setup` と `blocked` を1つにしない

| | 意味 | 画面 |
|---|---|---|
| `setup` | **質問を出していて、答えを待っている** | 「取得しますか？」＋ 取得する / 今は取得しない |
| `blocked` | **答えは終わっていて、不足が残っている** | 不足項目と解決方法 ＋ 終了 |

潰すと「どちらの画面を出すか」を **Stage が推測する**ことになり、判断が表示層に漏れる。

#### 失敗したら、ユーザーが「再試行」を押せる

取得の失敗はほとんどが一時的なもの（ネットワーク断・配布元の不調・転送の切断）である。
**失敗を伝えたうえで「再試行」と「今は取得しない」を並べる**（原則2 に従い対等に）。

**再試行の回数に上限を置かない。** 押すのは常にユーザーであり、催促ではないため。
逆に **Lumi の側からの聞き直しはしない**（同じ起動の中で勝手に聞き直せば、それは催促になる）。

**取得の失敗をセットアップ完了として扱わない。** `failed` は `failed` のままで、`boot` は `blocked` に落ちる。

#### 3つとも「導入の状態 × プロセスの状態」を独立に持つ

| | 導入の状態 | プロセスの状態 |
|---|---|---|
| TTS | `tts_setup_state` | `engine_runtime`（4値） |
| LLM | `llm_setup_state` | `engine_runtime` を**共用**。ただし **`starting` は起こらない**（Lumi が起動しないので「起動中」という状態が存在しない） |
| STT | `stt_setup_state` | `engine_runtime` を**共用**。外部プロセスではないが、CTranslate2 のモデルロードに `starting` / `ready` / `failed` がある〔ADR-035〕 |

**enum を1つに統合しない。** `detected`（ユーザーが自分で入れた）は TTS にしか無く、
`model_missing`（エンジンはあるがモデルが無い）は LLM にしか無い。
統合すると**起こり得ない状態が型の上で表現可能になり**、その分岐を書く羽目になる。

---

## 3. 取得元のピン留め

**取得先は設定にしない。** ユーザーが URL を差し替えられると、それは「Lumi が任意の実行体を落としてくる機能」になる。

| 項目 | 値 |
|---|---|
| 対象 | AivisSpeech Engine（**Engine のみ**。GUI アプリの AivisSpeech は取得しない） |
| ライセンス | **LGPL-3.0 単独**〔2026-08-15 確認。VOICEVOX ENGINE のデュアルライセンスのうち LGPL-3.0 のみを継承〕 |
| 配布元 | GitHub Releases `Aivis-Project/AivisSpeech-Engine` |
| バージョン | ピン留めする（Provisional。Lumi のリリースごとに更新する） |
| 実体 | `core/lumi/setup/engines.py` の `AIVISSPEECH_ENGINE` |

ピン留めする値は **バージョン / URL / サイズ / SHA-256** の4つ。

> **VOICEVOX は取得しない。** ソフトウェア利用規約が無断再配布を禁じており、
> 「Lumi が自動で取ってくる」形が許諾の範囲内かを判断できないため（fail-closed）。
> **ユーザーが自分でインストールしたものを検出して使う**（§6）。

### 3b. STT モデル〔Phase 1。決定 → [ADR-023](../decisions/ADR-023-llm-runtime-and-model-acquisition.md)〕

| 項目 | 値 |
|---|---|
| 対象 | faster-whisper の CTranslate2 変換済みモデル |
| 配布元 | HuggingFace。**リポジトリはモデルごとに違う**（`Systran/...` / `dropbox-dash/...`）。実体は下記 |
| ライセンス | **MIT**（OpenAI Whisper の重みを CTranslate2 に変換したもの。変換物も MIT で配布されている） |
| 実体 | `core/lumi/setup/models.py` の `STT_MODELS`。既定は `large-v3-turbo`（[ADR-027](../decisions/ADR-027-stt-model-large-v3-turbo.md)） |
| どれを取るか | **`settings.stt_model` が選んだもの。** 固定しない（下記） |

#### 取りに行くものと、Provider が探すものを一致させる

**取得対象は `settings.stt_model`（`LUMI_STT_MODEL`）から解決する。**
`FasterWhisperProvider` はこの設定から作られるので、セットアップ側が別のモデルを
決め打ちにすると、**入れたものと探すものがずれる**。ずれた結果は
「STT は `installed`、boot は `ready`、しかし喋っても何も起きない」であり、
[ADR-027](../decisions/ADR-027-stt-model-large-v3-turbo.md) が戻り道として残した
`LUMI_STT_MODEL=small` は**まさにそれを必要とする人のところで黙って効かなかった**。

**ピン留めされていない名前が選ばれたら、代わりに別のモデルを取らない。**
`not_configured`（`reason: unpinned_model`）として提示し、取得も尋ねない。
別のものを取れば、それは同じずれを一段先に送るだけである。

**ファイル構成はモデルごとに違う。** `small` は `vocabulary.txt`、`large-v3-turbo` は
`vocabulary.json` と `preprocessor_config.json` を持つ。`ModelArtifact` が**ファイル単位で**
ピン留めしているのはこのためで、「どのモデルも同じ5ファイル」を仮定しない。

**配布元の組織名は改名されうる。** `large-v3-turbo` は faster-whisper 内部の表
（`mobiuslabsgmbh/...`）と現在の id（`dropbox-dash/...`）が食い違っている〔2026-08-17〕。
**ピン留めするのは現在実在する方**であり、旧名が消えても commit sha と SHA-256 で
**失敗するだけで、違うものが入ることはない**。

**エンジンと形が違う。** 単一のアーカイブではなく**複数ファイルのディレクトリ**である。
したがってピン留めは**ファイルごとに サイズ + SHA-256**、加えて**リビジョン**を固定する。

| ピン留めするもの | なぜ |
|---|---|
| **リビジョン**（git commit sha） | `main` のままだと、**同じ URL の中身が後から変わる**。タグではなくコミットで固定する |
| ファイルごとの サイズ + SHA-256 | 1ファイルでも欠けたり壊れたりすれば STT は動かない。**部分的に揃った状態を「入っている」と扱わない** |

**HuggingFace は LFS のファイルにしか SHA-256 を公開していない。**
小さいファイル（`config.json` 等）は git の blob id（SHA-1）しか無いので、
**こちら側で1度取得して計算した値をピン留めする**。保証の意味はエンジンと同じで、
「**ピン留めした時点の配布物と、いま落としたものが同一である**」ことだけである。

#### 展開しない

モデルはアーカイブではないので `bsdtar` を通さない。**取得したファイルをそのまま置く。**
原子性の確保は §5 と同じ（一時ディレクトリに全ファイルを揃えてから rename 1手）。

#### ライブラリの自動ダウンロードは無効にする

`faster-whisper` / `huggingface_hub` は**モデルが無ければ黙って取りに行く**。
原則1（ユーザーが選ぶまで外部通信しない）が**ライブラリの都合で迂回される**ので、
`local_files_only` を立ててキャッシュに無ければ明示的に失敗させる。

---

## 4. 検証

**3つすべてを通らなければインストールしない。** 1つでも欠けたら中止してロールバックする。

| # | 検証 | 何を防ぐか |
|---|---|---|
| 1 | **URL のホストと配布元パスの固定**（`github.com/Aivis-Project/AivisSpeech-Engine/releases/download/...`）+ HTTPS | 別の配布元にすり替えられること。リダイレクト先も同様に検査する |
| 2 | **サイズ一致** | 途中で切れた取得物を掴むこと |
| 3 | **SHA-256 一致**（ピン留めした値と、取得したバイト列から計算した値） | 取得後に配布物が差し替えられること・転送中の改変 |

### ★ エンジン自身が、初回起動時にさらに外部から取得する〔2026-08-15 実測〕

**取得物の検証は「Lumi が落としてくるもの」にしか効かない。**
AivisSpeech Engine は**初回起動時に、エンジン自身の判断で**次を取得する。

| 取得元 | 内容 |
|---|---|
| `api.aivis-project.com`（AivisHub） | 既定の音声合成モデル（AIVMX） |
| HuggingFace | BERT モデル・トークナイザ |

これは Lumi の検証経路の外側であり、**Lumi は内容もハッシュも検証していない**。

設計上の帰結:

1. **同意の文言は「エンジンを取得する」だけでは足りない。** 初回起動時にモデルも取得されることを
   セットアップの説明に書く（実装: `stage/src/setup/SetupPanel.tsx`）
2. AivisHub からのモデル取得には **Aivis Project 利用規約が適用される**（[../licensing.md](../licensing.md) §3）。
   取得するのはユーザーの PC 上のエンジンであり、Lumi が代行するのではない
3. 取得された既定モデルは **ACML 1.0**〔2026-08-15 実測 → [../licensing.md](../licensing.md) §4.4〕。
   クレジット画面には、この経路で入る音声モデルがあることを書く必要がある
4. **ネットワークが無い環境ではエンジンの初回起動が失敗しうる。** 取得は成功したのに喋れない、
   という状態が起こる。Phase 1 で「エンジンは入っているが使えない」を状態として区別する

### 失敗は必ず「言える理由」になる〔2026-08-20 追加〕

**取得の失敗は、原則4（黙って劣化しない）が最も効くべき場所である。**
そこで `SetupError.reason` を出すのだが、**理由が付かない失敗があると、
ユーザーは「想定外のエラーが起きました」だけを渡されて手詰まりになる。**

| 起きたこと | reason | ユーザーがすること |
|---|---|---|
| ネットワークが切れた・タイムアウト・TLS 失敗（`httpx.HTTPError` 全般） | `network_unreachable` | **再試行**（ほとんどはこれで直る） |
| 書き込めない（容量不足・権限・使用中。`OSError` 全般） | `disk_error` | 空き容量とアクセス権を確認する |
| 検証に失敗した | `size_mismatch` / `hash_mismatch` ほか | 再試行 |
| **上記のいずれでもない** | `unexpected_error` | — |

**`unexpected_error` は「分類し損ねた」の印である。** 出たら、それは提示の問題として直す。

> ★ **実際に起きた**〔2026-08-20〕。音声認識モデル（480MB）の取得が途中で切れたとき、
> `httpx` の例外を理由に変換する経路がどこにも無く、Coordinator の catch-all が
> `unexpected_error` にしていた。**`network_unreachable` は UI が訳語を持ちながら
> Core が一度も出せない理由だった。**
>
> 見逃した原因は**テストの側にある**。取得失敗のテストが
> `SetupError("network_unreachable")` を**自分で投げて**代用していたので、
> 「`httpx` の例外 → 理由」の経路は一度も実行されていなかった。
> §8 テスト6 が「`.tmp-*` が残らない」だけを見ていたのも同じ穴で、
> **実断線の試験は [../roadmap.md](../roadmap.md) の持ち越しとして未実施のままだった。**

### 保証しないこと

- **ピン留めした SHA-256 は、GitHub の Release Asset API が返す `digest` を採ったものである。**
  配布元そのものが最初から改竄されていた場合は検出できない。
  検証が意味を持つのは「**ピン留めした時点の配布物と、いま落としたものが同一である**」ことについてのみ。
- **取得物が安全であることは保証しない。** ライセンス上再配布できないものを Lumi の配布物に入れないための仕組みであり、
  マルウェア検査ではない。
- **LGPL-3.0 の孫依存・同梱モデルのライセンスは未確認のまま**（[../licensing.md](../licensing.md) §7）。
  同梱しない限り Lumi の配布物には影響しないので、Phase 0 ではここまでとする。

---

## 5. 展開とインストール

### 展開に外部ライブラリを使わない

配布物は `.7z`（多ボリューム形式の第1ボリューム `.7z.001`）。単一ボリュームなので、**中身は完全な 7z ストリーム**である。

**Windows 内蔵の `bsdtar`（`%SystemRoot%\System32\tar.exe`, libarchive + liblzma）で展開する。**
Python の 7z ライブラリ（py7zr 等）は **LGPL であり、Core = MIT の境界に入れられない**。
外部プロセスとして OS 同梱のものを呼ぶことで、依存もライセンス上の問題も増やさない。

> `tar.exe` が無い環境（Windows 10 1803 未満）では**明示的に失敗**する。黙って別の手段を探さない。

### 原子的なインストール

```
%LOCALAPPDATA%\Lumi\engines\
├── .tmp-<random>\          ← ダウンロードと展開はすべてここ
└── aivisspeech-<version>\  ← 最後に rename で確定する。ここが在れば「完全に入っている」
```

**確定は rename の 1 手だけ。** 途中のどこで失敗しても、`.tmp-*` を消せば元に戻る。
ロールバックとは「確定前の状態を消すこと」であって、確定後の巻き戻しではない。

**確定先が「在るが不完全」なら、消してから rename する。** 中断されたインストールや
手で触られたディレクトリがこれにあたる。`rename` は既存の宛先を拒否するので、
**そのままでは再試行が永久に直せない**（毎回ダウンロードし直して最後の一手で失敗し、
ユーザーは誰にも知らされていないディレクトリを自分で消すしかない）。
消してから rename する間は「何も入っていない」状態になるが、
**消しているものは元から使えないもの**であり、置き換える側は検証済みで隣にある。

起動する実行体は展開後のツリーから `run.exe` を**探して**記録する（配布物の内部構造の変更に追随するため）。

---

## 6. 既存インストールの検出

**外部通信しない。** ローカルのパスと 127.0.0.1 だけを見る。

| エンジン | パス候補 | ポート |
|---|---|---|
| AivisSpeech | `%LOCALAPPDATA%\Programs\AivisSpeech\AivisSpeech-Engine\run.exe`, `C:\Program Files\AivisSpeech\AivisSpeech-Engine\run.exe` | 10101 |
| VOICEVOX | `%LOCALAPPDATA%\Programs\VOICEVOX\vv-engine\run.exe`, `C:\Program Files\VOICEVOX\vv-engine\run.exe` | 50021 |
| Lumi が入れたもの | `%LOCALAPPDATA%\Lumi\engines\aivisspeech-<version>\...\run.exe` | 10101 |
| **Ollama**〔Phase 1〕 | `%LOCALAPPDATA%\Programs\Ollama\ollama.exe`, `C:\Program Files\Ollama\ollama.exe`, および `PATH` | 11434 |

すでに起動しているエンジンがあれば、それを使う（二重に起動しない）。

> **Ollama のプロセスは Lumi が起動も停止もしない**（[ADR-023](../decisions/ADR-023-llm-runtime-and-model-acquisition.md)）。
> [core.md](core.md) §6 の「すでに動いているエンジンには触らない」を、**常にそちら側**に倒す。
> 検出できないケース（カスタムポート・リモートホスト・独自ビルド）は**設定で明示的に指定させる**。
> 検出を賢くして当てにいかない — 外すと「なぜか動かない」になる。

---

## 6b. 配布物が壊れていないかを、その PC で確かめる〔2026-08-15 追加〕

```
lumi-core.exe --self-check
```

sqlite-vec / FTS5 / PortAudio / TLS 証明書を、**実際に読み込んで**確認する。

**開発環境で通ることは、固めた後に通ることを意味しない。** ネイティブ拡張は
自動では同梱されず、しかも**入っていなくても import は成功する**。
実際に読み込むまで壊れていることが分からないため、**配布物を作った直後と、
別のマシンで動かない報告を受けたときに、これを回す。**

パッケージングの方式と理由 → [../decisions/ADR-021-sidecar-packaging.md](../decisions/ADR-021-sidecar-packaging.md)

---

## 7. Phase 0 でやらないこと

- **エンジンの自動更新**（ピン留めしたバージョンだけを扱う）
- **エンジンのアンインストール UI**（Phase 1 で設定 UI に入れる）

  > **「`%LOCALAPPDATA%\Lumi\engines\` を消せば済む」ではない。**〔2026-08-15 実測〕
  > エンジンは初回起動時に取得した音声モデルと BERT キャッシュを
  > **`%APPDATA%\AivisSpeech-Engine\`（Lumi の管理外）**に置く。
  > アンインストール UI を作るときは両方を対象にする。Lumi が入れていない
  > エンジンを検出して使っていた場合は**消してはならない**（ユーザーの持ち物）。

- **音声モデルの取得**（既定 Content Pack にモデルを含めない → [../licensing.md](../licensing.md) §2）
- **`Provider.attribution()` との接続**（Phase 1。Phase 0 のクレジットは Stage 側に静的に置く）

---

## 8. テスト

| # | テスト |
|---|---|
| 1 | **ユーザーが選択するまで外部への接続が発生しない**（取得関数が呼ばれるまで HTTP クライアントを作らない） |
| 2 | 「今は取得しない」を選ぶと状態が `not_configured` になり、**`boot` が `blocked` になる**（起動しない）〔ADR-034 で変更〕 |
| 3 | SHA-256 が一致しないとき、インストールされず `failed` になる |
| 4 | サイズが一致しないとき、インストールされず `failed` になる |
| 5 | **ピン留めした配布元以外の URL を拒否する**（リダイレクト先を含む） |
| 6 | ネットワーク断で失敗したとき、`.tmp-*` が残らない |
| 6b | **ネットワーク断が `network_unreachable` として提示される**（`unexpected_error` にしない）。`httpx` の例外を**実際に投げて**確かめる — `SetupError` を代用しない〔2026-08-20〕 |
| 6c | **書き込みの失敗が `disk_error` として提示される**（ネットワークと区別する）〔2026-08-20〕 |
| 7 | 展開に失敗したとき、確定先のディレクトリが作られない |
| 8 | `tar.exe` が無いとき、明示的に失敗する（別の手段を探さない） |
| 9 | 既存の AivisSpeech / VOICEVOX を検出して `detected` になる |
| 10 | 検出処理が外部へ接続しない（127.0.0.1 とファイルシステムのみ） |
| 11 | 状態が `failed` のとき、`not_configured` と区別して表示される |
| 12 | **固めた実行体で `--self-check` が全項目成功する**（sqlite-vec / FTS5 / PortAudio / TLS） |
| 13 | **配布物に ASIO 版の PortAudio が入っていない**（`lumi-core.spec` がビルド時に落とす） |
| 14 | **Ollama 未導入で Lumi が起動し、`not_configured` が表示される**〔Phase 1〕 |
| 15 | **Ollama はあるがモデルが無いとき、`model_missing` になる**（`not_configured` と区別される）〔Phase 1〕 |
| 16 | **Ollama を Lumi が起動も停止もしない**（プロセス生成の経路が存在しない。静的検査）〔Phase 1〕 |
| 17 | **STT モデルが無いとき、ライブラリが勝手にダウンロードせず明示的に失敗する**〔Phase 1〕 |
| 18 | **Silero VAD が配布物に含まれ、オフラインで barge-in が成立する**〔Phase 1〕 |
| 19 | **確定先が不完全なとき、再インストールが直せる**（中断・手編集の跡に rename できず、再試行が永久に失敗しない）〔Phase 1〕 |
| 20 | **`LUMI_STT_MODEL` の上書きが、取得するモデルにも反映される**（Provider が探すものと一致する。ピン留めされていない名前では代わりに別のものを取らない）〔Phase 1〕 |
| 21 | **3つとも使えるときだけ `boot` が `ready` になる**（1つでも欠ければ `blocked`）〔ADR-034〕 |
| 22 | **取得に失敗したとき `ready` にならない**（失敗はセットアップ完了として扱われない）〔ADR-034〕 |
| 23 | **`blocked` のとき、不足している要素すべてが解決方法つきで表示される**〔ADR-034〕 |
| 24 | **失敗のあとユーザーが再試行でき、成功すれば `blocked` を抜ける**（上限なし）〔ADR-034〕 |
| 25 | **`blocked` のあいだ音声入力が開かない**（ADR-033 のゲートが `ready` のままであること）〔ADR-034〕 |
| 26 | **答えを永続化しない**（不足したまま「今は取得しない」を選んでも、次の起動でまた尋ねる）〔ADR-034〕 |
| 27 | **不足が確定している間は `starting` を出さない**（「今は取得しない」の直後に `blocked` になる。エンジンの起動を待たせない）〔2026-08-20〕 |
| 28 | **起動中のエンジンは `blocked` の画面に並ばない**。ただし**起動に失敗すれば並ぶ**（裏では起動を試みている）〔2026-08-20〕 |
| 29 | **STT のロード失敗は `installed × runtime: failed` になり、取得失敗の `state: failed` と区別される**〔ADR-035〕 |
| 30 | **STT は `installed × runtime: ready` のときだけ使える**。ロード中は `starting`、失敗後は `blocked` になり音声入力を開かない〔ADR-035〕 |
